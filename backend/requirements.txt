# ── Core framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-dotenv>=1.0.1

# ── LangGraph + LangChain ──────────────────────────────────────────────────────
langgraph>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0       # DuckDuckGo wrapper + other community tools

# ── LLM providers (install only what you use) ──────────────────────────────────
langchain-openai>=0.1.0          # LLM_PROVIDER=openai or openrouter (same package)
langchain-google-genai>=1.0.0    # LLM_PROVIDER=gemini
langchain-ollama>=0.1.0          # LLM_PROVIDER=ollama

# ── Search providers ───────────────────────────────────────────────────────────
lxml>=5.3.0                      # Parses DuckDuckGo HTML results
tavily-python>=0.3.0             # SEARCH_PROVIDER=tavily
requests>=2.32.0                 # SEARCH_PROVIDER=serpapi (uses requests directly)
